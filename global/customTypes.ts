// An event is an action that a robot can perform.
// On a timeline, this would be a single point for each time it is preformed
export enum ECustomEvents {}

// A state is an action that a robot performs over an interval of time
// On a timeline, this would be an interval between two points in time
export enum ECustomStates {}
